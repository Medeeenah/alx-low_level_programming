#include <stdio.h>
/**
 * main - prints text with printf
 *
 * Return: 0 that ends the progra
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
